.tmp/
.DS_Store

# Artifacts left behind by the "Try It in 60 Seconds" quickstart when run at
# the repo root (download, checksum, extracted dir, and the hello program).
# Root-anchored so they don't shadow assets/wado-*.png.
/wado-*.tar.gz
/wado-*.zip
/wado-*/
/SHA256SUMS.txt
/hello.wado
